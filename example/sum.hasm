# 1からNまでの総和を求める

$set    0 . 10
$set    1 . 0
$use    0
$jmpf   5
$use    0
$add    1 . @ * 10
$add    0 . -1
$jmp    -5
$use    1
$set    1 . @ / 10
$use    1

# メモリ0に値10をセット
# メモリ1に値0をセット
# 一時レジスタにメモリ0の値をセット
# 比較結果がfalse(=一時レジスタの値が0)なら5命令ジャンプ
# 一時レジスタにメモリ0の値をセット
# メモリ1に一時レジスタの値を10倍した結果を加算
# メモリ0に-1を加算
# -5命令ジャンプ
# 一時レジスタにメモリ1の値をセット
# メモリ1に一時レジスタを10で割った結果をセット
